#include<iostream>
using namespace std;

int main(){

	// making a bucket in c++ of int type with a valid input name as per the naming rules
    int a; 

    // take input from user in a
    cout<< "enter the value of a:";
    cin>> a;

    // output of the taken input

    cout<< "the value of a is:" << a;

	return 0;
}
